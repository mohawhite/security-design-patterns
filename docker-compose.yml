version: '3.8'

services:
  security-app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./logs:/app/logs
      - ./database.sqlite:/app/database.sqlite
    environment:
      - NODE_ENV=production
    env_file:
      - .env
    restart: unless-stopped
